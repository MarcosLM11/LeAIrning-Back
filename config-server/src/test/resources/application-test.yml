spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/test-config-repo

eureka:
  client:
    enabled: false

encrypt:
  key: test-encrypt-key-minimum-16-chars