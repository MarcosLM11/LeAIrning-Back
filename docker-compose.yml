services:
  # Infrastructure
  postgres:
    image: 'postgres:latest'
    container_name: learning-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql
      - ./init-databases.sql:/docker-entrypoint-initdb.d/init-databases.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

#  qdrant:
#    image: 'qdrant/qdrant:dev'
#    container_name: learning-qdrant
#    ports:
#      - '6334:6334'
#      - '6333:6333'
#    volumes:
#      - qdrant-data:/qdrant/storage

#  minio:
#    image: minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1
#    container_name: leairning-minio
#    ports:
#      - "9000:9000"
#      - "9001:9001"
#    environment:
#      MINIO_ROOT_USER: minioadmin
#      MINIO_ROOT_PASSWORD: minioadmin
#    command: server /data --console-address ":9001"
#    volumes:
#      - minio-data:/data
#    healthcheck:
#      test: ["CMD", "mc", "ready", "local"]
#      interval: 30s
#      timeout: 20s
#      retries: 3

#  ollama:
#    image: ollama/ollama:latest
#    container_name: leairning-ollama
#    ports:
#      - "11434:11434"
#    volumes:
#      - ollama-data:/root/.ollama

  kafka:
    image: 'apache/kafka:latest'
    container_name: leairning-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT'
      KAFKA_LISTENERS: 'CONTROLLER://:29093,PLAINTEXT_HOST://:9092,PLAINTEXT://:19092'
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT_HOST://localhost:9092,PLAINTEXT://kafka:19092'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:29093'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      CLUSTER_ID: '4L6g3nShT-eMCtK--X86sw'
    volumes:
      - kafka-data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092"]
      interval: 10s
      timeout: 10s
      retries: 5

  # Application
#  backend:
#    image: leairning-back:latest
#    container_name: leairning-backend
#    restart: unless-stopped
#    ports:
#      - "8080:8080"
#    env_file:
#      - .env
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/leairningdb
#      LEAIRNING_STORAGE_MINIO_ENDPOINT: http://minio:9000
#      SPRING_AI_VECTORSTORE_QDRANT_HOST: qdrant
#      SPRING_PROFILES_ACTIVE: openai
#      SPRING_AI_OLLAMA_BASE_URL: http://ollama:11434
#      LEAIRNING_AUTH_FRONTEND_URL: http://localhost:4200
#      OAUTH2_REDIRECT_BASE_URL: http://localhost:4200
#    depends_on:
#      postgres:
#        condition: service_healthy
#      minio:
#        condition: service_healthy

#  frontend:
#    image: leairning-front:latest
#    container_name: leairning-frontend
#    restart: unless-stopped
#    ports:
#      - "4200:80"
#    depends_on:
#      - backend

volumes:
  postgres-data:
#  qdrant-data:
#  minio-data:
#  ollama-data:
  kafka-data:
